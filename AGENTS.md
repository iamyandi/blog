# 闫珶个人博客 - 需求拆解文档

## 产品概述

- **产品类型**: 个人博客网站
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 技术同行、艺术爱好者、潜在雇主/合作伙伴、个人读者
- **核心价值**: 展示闫珶的技术能力、艺术创作与个人生活，建立个人品牌影响力
- **界面语言**: 中文
- **主题偏好**: user_specified（深蓝色/科技蓝主色调，支持深色/浅色模式切换）
- **导航模式**: 路径导航
- **导航布局**: Topbar（消费者前台博客，面向公众读者）

---

## 页面结构总览

> **说明**：此表为页面生成的唯一数据源，包含所有页面（一级+二级）

| 页面名称 | 文件名 | 路由 | 页面类型 | 入口来源 |
|---------|-------|------|---------|---------|
| 首页 | `HomePage.tsx` | `/` | 一级 | 导航 |
| 文章分类页 | `CategoryPage.tsx` | `/category/:slug` | 一级 | 导航（分类下拉/标签云点击） |
| 文章详情页 | `ArticlePage.tsx` | `/article/:id` | 二级 | 首页文章卡片点击 / 分类页列表项点击 / 归档页时间轴项点击 / 搜索结果项点击 |
| 关于我 | `AboutPage.tsx` | `/about` | 一级 | 导航 |
| 归档页 | `ArchivePage.tsx` | `/archive` | 一级 | 导航 |
| 搜索结果页 | `SearchPage.tsx` | `/search?q=:keyword` | 二级 | 导航栏搜索框提交 |

> **页面类型说明**：
> - **一级页面**：出现在导航中，用户可直接访问
> - **二级页面**：不在导航中，从一级页面跳转进入

---

## 页面布局建议

- **首页布局模式**: 上下分区（Hero 个人简介区 → 最新文章列表 → 技术标签云），视觉重心在 Hero 区与文章列表，首屏即展示核心身份与最新内容
- **文章详情页布局模式**: 主从布局（桌面端左侧文章正文 + 右侧目录导航，移动端上下堆叠），视觉重心在文章正文阅读区；结果承载区为文章正文渲染区（含代码高亮、评论区）
- **分类页/归档页/搜索结果页布局模式**: 上下分区（页面标题 + 文章列表/时间轴），视觉重心在列表内容
- **关于我页面布局模式**: 上下分区（个人履历 → 技能树 → 联系方式），视觉重心在履历与技能展示

---

## 导航配置

- **导航布局**: Topbar（顶部固定，消费者前台博客）
- **导航项**（仅一级页面）:

| 导航文字 | 路由 | 图标(可选) |
|---------|------|-----------|
| 首页 | `/` | Home |
| 技术博客 | `/category/tech` | Code |
| 艺术创作 | `/category/art` | Palette |
| 生活随笔 | `/category/life` | Coffee |
| 归档 | `/archive` | Archive |
| 关于我 | `/about` | User |

> **搜索入口**: Topbar 右侧常驻搜索框，提交后跳转至 `/search?q=keyword`

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 文章列表（首页/分类/归档/搜索） | demo-mock | `src/data/articles.ts` 导出示例文章数组，含 id、标题、摘要、分类、标签、日期、内容（Markdown）、封面图等字段 | ✅ 本身就是 mock，含 6-8 篇覆盖三个分类的示例文章 |
| 文章详情 | demo-mock | 根据路由 `:id` 从 `articles` 数组中查找对应文章 | ✅ 同上 |
| 个人履历/技能树/联系方式 | demo-mock | `src/data/profile.ts` 导出个人资料对象 | ✅ 本身就是 mock |
| 技术标签云 | demo-mock | 从 `articles` 数组中聚合所有标签及出现频次 | ✅ 基于 mock 文章数据计算 |
| 搜索功能 | demo-mock | 前端对 `articles` 数组按标题、摘要、标签做关键词匹配过滤 | ✅ 基于 mock 文章数据过滤 |
| 深色/浅色模式切换 | local-persist | `localStorage` key=`__blog_theme`，值为 `'light'` 或 `'dark'`，初始化读取系统偏好 | 无（直接读取系统偏好作为默认值） |
| 评论数据 | demo-mock | `src/data/comments.ts` 导出示例评论数组，关联文章 id | ✅ 本身就是 mock，每篇文章含 2-4 条示例评论 |

> 类型选择 + 兜底约束见上方"数据来源声明方法论"段。

---

## 功能列表

> **说明**：每个页面/区块的功能点，供页面生成使用

- **全局功能（所有页面共享）**:
  - **页面目标**: 提供一致的导航与体验
  - **功能点**:
    - **固定顶部导航栏**: Topbar 固定在视口顶部，包含 Logo（闫珶）、导航项（首页/技术博客/艺术创作/生活随笔/归档/关于我）、搜索框、深色/浅色模式切换按钮；移动端折叠为汉堡菜单
    - **深色/浅色模式切换**: 点击 Topbar 中的切换按钮，在 `light` 和 `dark` 模式间切换，状态持久化到 `localStorage`（key=`__blog_theme`），默认跟随系统偏好
    - **返回顶部按钮**: 页面滚动超过一屏后，右下角显示圆形返回顶部按钮，点击平滑滚动至顶部
    - **页脚**: 显示版权信息、社交链接（GitHub 等）

- **首页 (`/`)**:
  - **页面目标**: 让访客快速了解闫珶是谁、写了什么、擅长什么
  - **功能点**:
    - **个人简介 Hero 区**: 展示姓名"闫珶"、一句话身份描述（IT从业者 / 陶瓷艺术设计背景 / 画家 / 插画师）、所在地上海、简短自我介绍文案，搭配艺术元素点缀（如手绘风格装饰线条或陶瓷纹理背景）
    - **最新文章列表**: 按发布日期倒序展示最新 5-6 篇文章卡片，每张卡片含封面图（或占位图）、标题、摘要、分类标签、发布日期；卡片带 hover 悬停动效（轻微上浮 + 阴影加深）；点击跳转至文章详情页
    - **技术标签云**: 展示所有文章标签的聚合云，标签字号按出现频次缩放，点击标签跳转至对应分类页或触发搜索

- **文章分类页 (`/category/:slug`)**:
  - **页面目标**: 按分类浏览文章
  - **功能点**:
    - **分类标题**: 根据 `slug` 显示分类名称（技术博客 / 艺术创作 / 生活随笔）及文章数量
    - **文章列表**: 展示该分类下所有文章卡片（同首页卡片样式），按日期倒序排列
    - **空状态**: 若分类下无文章，显示"暂无文章"提示

- **文章详情页 (`/article/:id`)**:
  - **页面目标**: 提供沉浸式阅读体验
  - **功能点**:
    - **文章正文渲染**: 将 Markdown 内容渲染为 HTML，支持代码块语法高亮（使用 Prism.js 或 highlight.js），代码块右上角显示"复制"按钮，点击复制代码到剪贴板并 toast 提示"已复制"
    - **目录导航**: 桌面端右侧固定目录导航（TOC），根据文章标题层级（h2/h3）自动生成，点击目录项平滑滚动至对应段落，当前阅读位置高亮对应目录项；移动端目录折叠为悬浮按钮
    - **评论区**: 文章底部展示评论区，含已有评论列表（头像、昵称、时间、内容）和评论输入框（昵称、邮箱、内容），提交后追加到评论列表并 toast 提示"评论已提交"（纯前端 mock，不持久化）
    - **文章元信息**: 顶部展示文章标题、作者、发布日期、分类标签、阅读时长估算

- **关于我页面 (`/about`)**:
  - **页面目标**: 全面展示个人背景与能力
  - **功能点**:
    - **个人履历时间轴**: 按时间线展示教育背景（陶瓷艺术设计专业）、职业经历（6年Java开发 / 甲方央企银行）、其他身份（画家 / 插画师 / 艺考素描美术老师）
    - **技能树**: 分组展示技术栈（Java后端、Spring Boot、Linux运维、Python自动化、Docker、MySQL）和艺术技能（陶瓷设计、绘画、插画、素描教学），使用进度条或标签云形式
    - **联系方式**: 展示邮箱、GitHub 链接、所在地（上海）等社交/联系方式
    - **个人生活**: 简短提及健身减脂、网文创作、金融投资等爱好，以及女儿"潇潇"（2024年6月3日出生）

- **归档页 (`/archive`)**:
  - **页面目标**: 按时间线浏览所有文章
  - **功能点**:
    - **时间轴展示**: 按年份分组，每条记录显示日期、文章标题（可点击跳转详情）、分类标签
    - **年份快速跳转**: 侧边或顶部显示年份列表，点击滚动至对应年份区块

- **搜索结果页 (`/search?q=:keyword`)**:
  - **页面目标**: 展示关键词匹配的文章
  - **功能点**:
    - **搜索框**: 顶部显示搜索关键词，可修改后重新搜索
    - **搜索结果列表**: 展示匹配的文章卡片（标题、摘要、分类、日期），按相关度或日期排序
    - **空状态**: 无匹配结果时显示"未找到相关文章"提示，建议更换关键词

---

## 数据共享配置

| 存储键名 | 数据说明 | 使用页面 |
|---------|---------|---------|
| `__blog_theme` | 主题模式，类型为 `'light' \| 'dark'` | 全局（所有页面） |

> 基础类型（`string` 字面量联合）不需要定义 interface。

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 无参考材料，从产品语义与内容情绪出发自主建立视觉方向
- **核心情绪 / 应用类型**: 一个兼具技术严谨与艺术感知的个人博客，需要在代码秩序与创作温度之间找到平衡点
- **独特记忆点**: 代码块的终端深色与陶瓷釉面的温润光泽在同一页面中对话——技术区冷峻精密，艺术区留白呼吸，用材质对比而非装饰堆砌来体现双重身份

## 2. Art Direction

- **方向名**: 窑变与终端
- **Design Style**: Swiss Minimalist + Warm Natural 辅助 —— Swiss 提供技术博客需要的网格秩序、清晰层级和高可读性；Warm Natural 为艺术和生活内容引入纸张温度与手工质感，避免博客沦为又一个冷冰冰的技术文档站
- **DNA 参数**: 圆角 subtle（`rounded-md`）/ 阴影 subtle（`shadow-sm`，卡片 hover 升至 `shadow-md`）/ 间距 spacious（`gap-6` `p-8`）/ 字体方向 无衬线正文 + 衬线标题点缀 / 装饰手法 极简线条插画 + 陶瓷釉色微光点缀
- **应用类型**: Content —— 阅读优先，信息层级清晰，长文舒适

## 3. Color System

**色彩关系**: 深蓝墨主色 + 暖灰纸白背景 + 陶瓷青釉 accent + 终端深色代码区
**配色设计理由**: primary 深蓝墨承载技术身份和主交互，不刺眼、不轻浮，符合银行 IT 从业者的稳重气质；accent 取自青瓷釉色，低饱和、有手工感，用于 hover 状态和艺术分类标记，是"艺术元素点缀"的具体落点；bg 用暖调纸白而非纯冷白，为生活随笔和育儿内容保留温度；代码区独立使用深色终端底，与正文区形成材质对比
**主色推导**: 用户要求"深蓝色/科技蓝"，但纯科技蓝过于通用 SaaS。将蓝向墨色方向压低明度、降低饱和，得到更接近墨水与深夜屏幕的颜色，同时与陶瓷青釉 accent 形成冷暖微对比
**使用比例**: 60% 中性（bg + card + border）/ 30% 辅助（accent 浅底 + 代码区深色）/ 10% primary（CTA、链接、当前页标记、品牌锚点）

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(40 20% 97%) | 暖调纸白，带极微黄，模拟书籍纸张 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 纯白卡片，在暖底上形成清晰承载面 |
| text | `--foreground` | `text-foreground` | hsl(220 15% 15%) | 深蓝墨正文，高对比可读 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(220 8% 46%) | 辅助信息、日期、标签副文 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(218 45% 28%) | 深蓝墨主色，链接、CTA、当前导航 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 98%) | primary 上的白色文字 |
| accent | `--accent` | `bg-accent` | hsl(175 20% 88%) | 青瓷釉浅底，hover/focus/选中态 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(178 30% 28%) | accent 上的深青文字，标签、分类标记 |
| border | `--border` | `border-border` | hsl(220 10% 88%) | 浅灰蓝边框，轻量分隔 |

**语义色提示**:
- 成功: `hsl(160 40% 35%)` (bg: `hsl(160 30% 92%)` / border: `hsl(160 25% 78%)` / text: `hsl(160 40% 28%)`) —— 低饱和绿，与 primary 冷调一致
- 警告: `hsl(38 50% 50%)` (bg: `hsl(38 40% 92%)` / border: `hsl(38 35% 78%)` / text: `hsl(38 50% 35%)`) —— 暖琥珀，与暖白 bg 协调
- 错误: `hsl(0 40% 48%)` (bg: `hsl(0 30% 94%)` / border: `hsl(0 25% 82%)` / text: `hsl(0 40% 38%)`) —— 低饱和红，不刺眼
- 语义色饱和度与 primary 对齐在 ±15% 内，不跳脱整体克制调性

## 4. 字体与节奏

- **font-display**: Noto Serif SC —— 用于文章标题和首页大标题，衬线带来书籍与人文温度，呼应"艺术元素点缀"需求
- **font-body**: Noto Sans SC —— 正文清晰可读，适合技术长文和代码相邻场景，与 Inter 同气质但中文渲染更优
- **字号**: H1 `text-4xl md:text-5xl`；H2 `text-2xl md:text-3xl`；body `text-base`；muted `text-sm`
- **圆角**: subtle（`rounded-md`）—— 卡片和按钮微圆，保持现代感但不软萌，代码块用 `rounded-md` 与整体一致

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导——首页（简介+文章列表+标签云）、分类列表、文章详情、关于我、归档、搜索
- **Page / Section Order**: 导航固定顶栏 → 页面主体 → 页脚；首页自上而下：Hero 简介区 → 最新文章卡片网格 → 标签云 → 页脚
- **Standard Content Zone**: `max-w-3xl` + `mx-auto` —— 博客阅读最佳行长 65-75 字符，3xl 保证正文舒适；文章详情页收窄至 `max-w-2xl` 以优化长文阅读
- **Shell / Frame Alignment**: 内容容器与导航同宽，导航内部也受 `max-w-3xl` 约束，左右留白一致
- **Padding & Rhythm**: `px-4 md:px-6 py-12 md:py-16`，段落间距 `space-y-6`，卡片网格 `gap-6`
- **Full-bleed Zones**: 首页 Hero 区背景可全宽（暖灰底或极简几何线条装饰），内部文字和 CTA 仍受 Standard Content Zone 约束；代码块在文章详情中可略微超出正文栏至 `max-w-3xl`，但不全宽
- **Local Narrowing**: 文章详情正文区收窄至 `max-w-2xl`；关于我页面的技能树和履历保持 `max-w-3xl`
- **Overflow Strategy**: 宽代码块、横向表格使用 `overflow-x-auto`；目录导航在移动端折叠为抽屉
- **Flexibility Boundary**: 允许移动端调整 padding 和卡片堆叠方向；全局 max-w、圆角系统、主色和阴影语言保持一致

## 6. 视觉与动效

- **装饰**: 青瓷釉色细线插画 + 几何线条分隔 —— 首页 Hero 可用极简线条勾勒陶瓷轮廓或代码符号，不铺大图；分类标签用 accent 浅底微光
- **阴影/边界**: 轻 —— 卡片默认 `shadow-sm`，hover 升至 `shadow-md`，模拟纸张浮起；代码区用深色背景 + 微弱内阴影区分
- **动效**: 克制 —— 卡片 hover 上浮 4px + 阴影加深，过渡 200ms ease-out；导航下划线滑动；深色/浅色模式切换使用 300ms 背景色渐变；返回顶部按钮淡入淡出

## 7. 组件原则

- 按钮、表单、菜单、卡片必须有 Default / Hover / Active / Focus / Disabled 状态
- Primary 按钮用 `bg-primary text-primary-foreground`，仅用于主 CTA（如"阅读更多"、提交评论）
- 文章卡片 hover：`shadow-md -translate-y-1`，左侧加一条 3px 的 primary 色带作为分类标记
- 代码块：深色终端底 `hsl(220 15% 10%)`，文字 `hsl(175 20% 80%)` 青瓷色调，复制按钮在右上角 hover 显示
- 标签云：标签用 accent 浅底 + accentForeground 文字，hover 切换为 primary 浅底
- 深色模式：bg 转为 `hsl(220 15% 8%)`，card 为 `hsl(220 12% 14%)`，text 为 `hsl(40 15% 90%)`，primary 提亮至 `hsl(218 40% 55%)`，保持相同色相体系

## 8. Image Direction

- **Image Role**: 首页 Hero 区可选极简插画（非必需）；文章封面图用于卡片缩略图；关于我页面可选个人形象插画或工作场景氛围图
- **Image Art Direction**: 若使用插画，风格为极简线条 + 低饱和青瓷釉色块，类似手工丝网印刷质感，主题围绕代码符号、陶瓷器型、画笔工具等个人符号的几何化表达；不使用照片或写实渲染，保持与整体 Swiss 秩序的协调
- **Image Prompt Keywords**: minimalist line art, celadon glaze color palette, geometric code symbols, ceramic vessel outline, brush and ink, warm off-white paper texture, silkscreen print style, subtle grain, editorial illustration
- **Image Avoidance**: 避免通用科技感插画（蓝紫渐变、电路板、抽象网络节点）、商务人物素材图、无主题抽象渐变图、写实摄影风格头像或场景

## 9. Anti-patterns

- **Split personality**: 技术区冷峻、艺术区温暖是材质对比，不是两套设计系统；全局 max-w、圆角、阴影、字体层级必须统一
- **Phantom tokens**: 不编造 shadcn/ui 不存在的 CSS 变量；深色模式变量在 tailwind-theme.css 的 `.dark` 中补齐
- **Default SaaS drift**: 避免回到默认蓝 `hsl(217 91% 60%)` 按钮和通用紫渐变卡片；始终使用深蓝墨 primary 和青瓷 accent
- **Invisible interaction**: 代码块复制按钮、标签云标签、归档时间轴节点必须有 `focus-visible` 环，颜色用 primary
- **Mono-hue tyranny**: primary 深蓝墨只用于链接、CTA、当前导航标记和分类色带；标签云、hover 态、选中态交给 accent 青瓷色系
- **Status color drift**: 成功/警告/错误色饱和度不高于 primary 的 15% 以上；深色模式下语义色需同步降低明度，避免刺眼
- **Code block isolation**: 代码区深色底不能与正文区形成刺眼的黑白切换；深色底明度与正文 bg 差距控制在 30% 以内，过渡柔和